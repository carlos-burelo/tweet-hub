import _ from './CommentModal.module.scss'

interface CommentModalProps {}

export default function CommentModal({}: CommentModalProps) {
  return <div className={_.container}>CommentModal Component</div>
}
